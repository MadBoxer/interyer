%h3 Оформить заказ

- if not @cart.items.empty?
  %table{ :border => 1, :cellpadding => 2, :id => "cart_table"}
    %thead
      %tr
        %th №
        %th Название
        %th Количество
        %th Сумма
        %th Удалить
      %tbody
        = render :template => 'public/cart/cart_table_body'
      %tfoot
        %tr
          %th{:colspan => "3"}
          %th= 'Итого: ' + @cart.total_price.to_i.to_s+' руб'
          
  %br
-if not @user
  %h4
    Вы не зарегистрированы. Рекомендуем 
    =link_to('создать аккаунт.', '/register')
= render 'form'


