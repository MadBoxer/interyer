<h1>Товары</h1>

<table>
  <tr>
  	<th>ID</th?>
  	<th>Категория</th?>
    <th>Название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @administration_products.each do |p| %>
  <tr>
    <td><%= p.id %></td>
    <td><%= p.name %></td>
    <td></td>
    <td><%= link_to 'Show', administration_product_path(p) %></td>
    <td><%= link_to 'Edit', edit_administration_product_path(p) %></td>
    <td><%= link_to 'Destroy', administration_product_path(p), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новый товар', new_administration_product_path %>|
<%= link_to 'Назад', '/administration' %>

